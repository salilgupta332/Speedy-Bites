apiVersion: v1
kind: Service
metadata:
  name: speedy-bites-nginx
  namespace: speedy-bites
spec:
  selector:
    app: speedy-bites-nginx
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer